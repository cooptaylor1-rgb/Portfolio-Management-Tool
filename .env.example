# Portfolio Management Tool - Environment Variables
# Copy this file to .env and fill in your actual credentials
# NEVER commit the .env file to version control!

# Backend API origin (NOT including /api/v2)
# Examples:
#   VITE_API_URL=http://localhost:3000
#   VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=http://localhost:3000

# Optional: override WebSocket URL for real-time market stream
# Defaults to derived from VITE_API_URL: ws(s)://<host>/api/v2/market/stream
# Example:
#   VITE_WS_URL=ws://localhost:3000/api/v2/market/stream
#   VITE_WS_URL=wss://api.yourdomain.com/api/v2/market/stream
# VITE_WS_URL=

# FactSet API Credentials
# Get your credentials from: https://developer.factset.com/
# You need a FactSet Developer Portal account
#
# IMPORTANT: Vite exposes VITE_* variables to the client bundle.
# Do NOT put real provider secrets here for production.
# For production, configure provider keys on the backend and proxy requests.
VITE_FACTSET_USERNAME=your_factset_username
VITE_FACTSET_API_KEY=your_factset_api_key

# OpenAI API Key
# Get your API key from: https://platform.openai.com/api-keys
# The app works with keyword search if no API key is provided
#
# IMPORTANT: Do NOT put real OpenAI keys here for production.
# Use a backend proxy endpoint to keep secrets server-side.
VITE_OPENAI_API_KEY=your_openai_api_key

# Note: In Vite, environment variables must be prefixed with VITE_
# to be exposed to the client-side code
# The .env file is already protected by .gitignore
